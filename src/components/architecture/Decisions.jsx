export default function Decisions({ project, language }) {
  return <section className="architecture-decisions" aria-label={language === 'es' ? 'Decisiones arquitectónicas' : 'Architecture decisions'}>{project.decisions.map((decision, index) => <article key={decision.id} className="architecture-decision"><span>{String(index + 1).padStart(2, '0')} / {decision.category}</span><i>{decision.kind === 'verified' ? (language === 'es' ? 'VERIFICADO' : 'VERIFIED') : (language === 'es' ? 'OBSERVACIÓN' : 'OBSERVATION')}</i><h2>{decision.title[language]}</h2><dl><div><dt>Context</dt><dd>{decision.context[language]}</dd></div><div><dt>{language === 'es' ? 'Solución observada' : 'Observed choice'}</dt><dd>{decision.choice[language]}</dd></div><div><dt>Trade-off</dt><dd>{decision.tradeoff[language]}</dd></div></dl><footer>{decision.evidence.map((path) => <code key={path}>{path}</code>)}</footer></article>)}</section>
}
